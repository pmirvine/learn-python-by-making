[project]
name = "pyxel-side-quest"
version = "0.1.0"
description = "A side quest: a complete game for the Pyxel fantasy console."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyxel>=2.9.9",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
