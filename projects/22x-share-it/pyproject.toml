[project]
name = "share-it"
version = "0.1.0"
description = "A bonus: Python in the browser, with PyScript and pygbag."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame-ce>=2.5.8",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
