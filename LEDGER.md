# Ledger

What each chapter *actually* taught, what it promised later chapters would explain, and what it handed on for reuse. `PLAN.md` is the intention; this is the record. Update it as each chapter is finished, and read it before writing the next.

Rules it exists to enforce:

- **Nothing is used before it's taught.** If a chapter needs a concept that isn't in an earlier "Taught" list, it teaches it or does without.
- **Every promise is kept.** When a chapter says "Project N explains this", it goes in N's "Owes" list here.
- **Reused code keeps its shape.** When a later project depends on an earlier one's package, the API recorded here is the contract.

## Project 0 · Hello, Beeb

**Taught:** `uv init --no-package`, `uv run`, what each generated file is; `.venv` and `uv.lock` ("commit the recipe, not the cake"); the REPL (`uv run python`, Tab, `help()`, `exit`); `def main()` and indentation as block structure, lightly; `import platform`; f-strings, first look; reading a traceback bottom-up; `git status`/`add`/`commit -m`/`log --oneline`; committing from VS Code's Source Control panel; imperative commit messages; PEP 723 scripts (`uv init --script`, `uv add --script`); `from x import y`; adjacent string literals; `\n`; `-> None` noticed but not explained. Type-in: ANSI colour bars (`end=""`, `{name:^8}`, nested `for` over `range`, a list literal, all unexplained).

**Owes:** P1 `**`, `/`, `//`. P3 type hints; `enumerate` as the "better way" than indexing `NAMES[colour]`. P4 `if __name__ == "__main__"` in full; `uv add` to a project. P5 packaged layout. P2 the debugger. P27 pip, venv and the older tools.

## Project 1 · Hi-Lo

**Taught:** names are labels, not boxes (assignment binds, never copies); dynamic but strong typing; everything is an object, methods with a dot; `type()`; `int()`, `float()`, `str()` conversions and `ValueError`; `input()` always returns `str`; `random.randint` is inclusive at both ends (flagged as the exception); `if`/`elif`/`else`; comparison and `and`/`or`/`not`; chained comparisons; f-strings with expressions, `:.2f`, `:,`, `!r`; `while True` + `break`; `continue`; augmented assignment, no `++`; `while`/`else`; conditional expressions; UPPER_CASE constants by convention; truthiness and the falsy values; `try`/`except ValueError`, never bare `except`; EAFP named and briefly justified; `/` vs `//` vs `%`, flooring towards minus infinity; `.bit_length()`; simple functions, `return`, implicit `None`; docstrings and `help()`; local vs global names, reading only; importing your own file at the REPL; `None`, `is None`/`is not None`; method chaining (`.lower().startswith()`); single vs double quotes. Git: `git diff`, `git add <file>`, `commit -am`, `git show`, `git diff A B`; the run–diff–commit rhythm. Type-in: tuple assignment and swap (`low, high = 1, 100`), binary search.

**Owes:** P2 ranges include the start and exclude the end; `for`; functions in depth. P3 tests for functions. P4 aliasing with mutable objects (two names, one list); `is` vs `==` in depth; `__name__` mechanism. P5 EAFP's full rationale. P7 scope, the rest of the story (LEGB, closures). Part 2: `%` for wrapping round screen edges (paid in P8).

## Project 2 · Turtle Sketchbook

**Taught:** `turtle.Screen()`/`turtle.Turtle()` as objects; don't name a file after a module (shadowing, with the 3.13+ error message); `for` as for-each, not a counting loop; `range` with 1, 2, 3 arguments, negative steps; start-inclusive/end-exclusive and why; `list(range(...))` just to look; `_` as a throwaway name; indexing with `[]` from 0, `len()`; `%` to cycle through a tuple; "don't write `range(len(...))`" (with a promise of `enumerate`); parameters, default values, defaults-after-non-defaults; keyword arguments, mixing positional and keyword, skipping defaults by name; functions built from functions and the "leaves things as it found them" contract; defaults are evaluated once at `def` time (shown with `random.randint`), and the `size=None` idiom; tuples: literal, indexing, `len`, unpacking, swap explained, `ValueError` on mismatch, `first, *rest`, immutability (`TypeError`), one-item `(5,)`, empty `()`; returning several values; tuple unpacking in `for` (`for x, height in ...`); RGB tuples; `random.uniform`, `random.choice` (in a Tweak), `random.seed` (in a Predict); recursion, base case, reasoning by trusting the contract, `RecursionError`; the call stack and per-call locals. Tools: VS Code debugger basics: breakpoint, F5, F10, F11, Shift+F11, Shift+F5, Variables and Call Stack panels, clicking frames. Extend hint shows passing a function without brackets (`screen.onkey(new_tree, "space")`) and module-level `pen`/`screen`.

**Owes:** P3 `enumerate` ("something better" than `range(len())`); slices follow the range convention; tests. P4 the mutable-default-argument gotcha ("evil twin, involving lists"); why immutability matters. P7 functions as values, and better ways than module-level `pen`. P16 a Logo interpreter.

**Infrastructure:** `tests/paperturtle.py` (a fake `turtle` that records and renders to PNG), `tests/turtle_runner.py` (runs a program against the real turtle, flat out, where a display exists), `scripts/screenshots_p02.py`.

## Project 3 · Dice Lab

**Taught:** lists: literal, `append`, index, negative index, `len`, `in`, item assignment, `.count()`; lists vs tuples (collection vs record); `min`/`max`/`sum`/`sorted` work on any collection; `sorted()` vs `.sort()`, and the in-place-methods-return-`None` convention; dicts: literal, lookup, assignment, `in` on keys, insertion order, `KeyError`, `.get(key, default)`, the `.get(k, 0) + 1` tally idiom, `.items()`, `.values()`, `.keys()`; keys must be unchangeable (reason deferred); `collections.Counter`, missing key is 0, `most_common`; format specs `:>3`, `:.1%`, `:^3`; list comprehensions, with `if`; dict comprehensions; "don't use a comprehension for side effects"; slices `[a:b:c]`, negative, omitted ends, `[::-1]`, `[:]` copies, slices never raise, same convention as `range`; `enumerate(…, start=1)`; `sorted(…, reverse=True)`; `zip`, `strict=True`, zip-with-offset trick; `itertools.pairwise`; empty list is falsy (`if not results`); functions as objects stored in a dict and called later (dispatch table); nested tuple unpacking in `for`; `str.join` used, explanation deferred; type hints: parameters, returns, `list[int]`, `dict[str, int]`, `tuple[int | None, int]`, `X | None`, `-> None`, `Callable[[], int]`, hints on variables; hints aren't enforced at run time; gradual typing; `bool` counts as 1/0 in `sum` (in a hint). Generator expressions seen in the type-in, unexplained. Tools: `uv add --dev`; pytest: `test_*.py`, `test_*` functions, bare `assert`, reading a failure report, `pytest.approx`, testing random code by properties and by `random.seed`, "return, don't print" for testability; VS Code Testing panel, Debug Test; `uv run ruff check`, rule codes, `ruff format`; ruff's RUF007 as a worked example. Rhythm is now run, test, lint, diff, commit.

**Owes:** P4 switch on a type checker (Pylance `basic`); why dict keys must be unchangeable (hashing); `str.join` "looks back to front, Project 4 explains"; `__name__` "takes the lid off". P6 generator expressions ("a bit more to it than that"). P7 floats, the whole story (`0.1 + 0.2`); functions as values taken "a long way further".

**Layout note:** flat project with `test_main.py` beside `main.py`; `scripts/check_all.py` handles both this and `tests/`.

## Project 4 · Codebreaker

**Taught:** strings as sequences (index, slice, `in` for substrings, iteration, no char type); immutability of `str`, methods return new strings; the everyday methods (`upper`/`lower`/`title`, `strip` family, `replace`, `split`, `join`, `startswith`/`endswith`, `find`, `count`, `isdigit`/`isalpha`/`isspace`); `join` belongs to the glue, and why; "don't build strings with `+=` in a loop"; Unicode: code points, `ord`/`chr`, `\N{…}`, `\u…`, `len` counts characters, `"straße".upper()`, `casefold`; `str` vs `bytes`, `encode`/`decode`, UTF-8 (Under the bonnet only); `random.choices(k=)`, `random.sample` (Tweak); sets: `set()`, literals, `-`, `&`, `|`, `<=`, uniqueness, fast `in`, unordered so `sorted()` for stable output, `{}` is a dict; `raise ValueError(msg)`, and why raise rather than return `None` or print; `except … as error`; writing tests before the code; `enumerate(zip(…))` with nested unpacking; **aliasing**: two names one list, `is` vs `==`, `is` only for `None`, small-int/str caching is an accident; copying (`.copy()`, `list()`, `[:]`); arguments are aliases; mutate-and-return-`None` vs return-new convention; `+=` on lists mutates in place (Under the bonnet); shallow vs deep copy, `copy.deepcopy`; the `[[x] * n] * m` trap and the comprehension cure; mutable default arguments (B006) and the `None` idiom; hashability: why dict keys/set items must be immutable, table of mutable vs immutable built-ins, `frozenset` named; `__name__` and `"__main__"` in full, importing runs the module, "dunder" named; `uv add` for a runtime dependency vs `--dev`; Rich: `Console`, markup, `console.input`; "work it out, don't store it" (board derived from history); `Counter & Counter` (Extend hint). Tools: Pylance `typeCheckingMode: "basic"`, reading a type error, checker as adviser; `git restore`, `git restore --staged`, `git commit --amend` (`--no-edit`), "only amend unshared commits". First bug hunt (format established: reproduce, failing test, fix; file in `bughunt/`, fix and test in `solutions/bughunt/`).

**Owes:** P5 "the good way" to reuse a function from another project (modules/packages); `Path(...).read_text()` previewed; structure for bigger programs. P6 tuples as dict keys/set members "a whole universe built on that"; pushing to GitHub (and why not to amend shared commits). P11 `bytes` for sound. P12 dunder names "become very important".

**Infrastructure:** `tests/conftest.py`'s `play` fixture now also fixes `random.choices`/`random.sample` (`code=`) and makes `time.sleep` instant. `scripts/screenshots_p04.py` uses Rich's `Console(record=True).save_svg()`.

## Project 5 · The Colossal Cupboard

**Taught:** the packaged layout (`uv init name`, `src/name/`, `__init__.py`, `[build-system]`, `[project.scripts]`, editable install, `uv run name`); uv copies Git name/email into `authors`; module vs package; absolute imports within a package; imports run the module once; avoiding circular imports by layering; no `__name__` guard needed in a package entry point; dataclasses: `@dataclass`, fields from hints, generated `__init__`/`repr`/`==`, defaults, `field(default_factory=…)` (tied to P4's mutable default), `dataclasses.replace`, `asdict`, mutable by default, `frozen=True` named for P10; "your first class: a dict with fixed named slots"; decorators named, deferred; `Enum`: members, `.value`, lookup by value, `ValueError`, iteration, hashable; dict merge `|`; dict comprehension building `DIRECTIONS`; passing a bound method as a factory (`START_PLACES.copy`); **the `respond(state, text) -> (new_state, reply)` design** and its four pay-offs; single source of truth (`places`); EAFP vs LBYL with the full rationale (race with files, can't get the check wrong, happy path reads straight); keep `try` blocks small; **`match`**: literal, capture, `|`, `*_`, guards, case order, works on str too, the bare-name-is-a-capture gotcha, no fall-through; `textwrap.fill`; tests in `tests/`; walkthrough test; testing that inputs aren't mutated; testing data integrity; `pathlib.Path` (`/`, `.name`, `.suffix`, `.parent`, `.exists`, `.open`, `.read_text`, `.write_text`); `with` and context managers (named; writing one is P18); file modes; always `encoding="utf-8"` (3.15 default noted); JSON (`dump`/`load`, `dumps`/`loads`, type mapping, no sets, string keys); `State(**data)` (full story P7); custom exception class; exception hierarchy, tuple of types, specific-before-general; `raise … from error` / `from None` (B904); `try`/`except`/`else`/`finally`; fixtures as parameters, `tmp_path`; `pytest.raises(…, match=…)` as a context manager. Git: `git switch -c`, `git switch`, `git merge` (fast-forward), `git branch -d`/`-D`, `git log --oneline --graph`; "a branch per feature and per challenge" from here on. `sys.argv`, `Path(__file__)` mentioned in challenges only.

**Hands on (the `cupboard` package, v0.1.0):** `cupboard.engine.State` (fields `location`, `places`, `unlocked`, `moves`, `won`; `State()` is a new game), `respond(state, text) -> tuple[State, str]` (pure), `describe(state) -> str`; `cupboard.world.ROOMS`, `ITEMS`, `Direction`, `DIRECTIONS`, `PLAYER`; `cupboard.saves.save(state, path)`, `load(path)`, `SaveError`. Front-end-only commands: `quit`/`q`, `save`, `restore`. Replies use `\n` between lines; lines starting with a space are preformatted. Used again by P21 (web) and P26 (Textual).

**Owes:** P6 `parametrize` (P5 deliberately uses a dict-and-loop in `test_replies_to_things_that_do_not_work`, so P6 can improve on it); `_private` naming; GitHub. P7 `**` in full. P9 classes beyond data. P10 `frozen=True`. P11 installing your own package into another project. P12 a merge that isn't a fast-forward. P16 writing decorators. P17 `match` on dataclasses. P18 writing a context manager.

## Project 6 · Life

**Taught:** choosing a data structure: set of `(x, y)` tuples vs list of lists (no edges, cost proportional to live cells, `in` is instant); set comprehensions; comprehensions with two `for`s (order as nested loops); `type Cell = …` alias, **first introduced here** (P8 meets it again); `_private` underscore convention; `itertools.product(repeat=)`; the "nudge your neighbours" `Counter` trick; why `step` must build a new set; **generators**: `yield`, calling runs nothing, runs to the next `yield` and freezes, `next()`, laziness, infinite `while True` is safe, single-pass exhaustion gotcha; the iteration protocol named; `Iterator[T]` hint; generator expressions, round brackets, brackets optional as sole argument, list-vs-genexp guidance; `itertools.islice` (count; start/stop/step; `None`), and the rest of `itertools` named; module-per-concern (`core`, `patterns`, `render`, `cli`), "only this module knows what a cell looks like"; triple-quoted strings; `random.Random(seed)` as un-shared randomness; `frozenset`, completing P4's hashability table; `normalise`/`period`; `__init__.py` as a front door with re-exports and `__all__`; `argparse`: positional with `nargs="?"`, options, `type=`, `default=`, `choices=`, `help=`, generated `--help`, `main(argv=None)` for testability; `[*iterable]` unpacking inside a list literal; `shutil.get_terminal_size`; ANSI home/clear/hide/show cursor; `KeyboardInterrupt`, `pass`, `finally` for clean-up; `sys.argv` (type-in); bit tricks `>>`, `& 1` seen in the type-in, unexplained. Tools: `@pytest.mark.parametrize` (tuples of names, ids, over a dict's keys), `-v`, `-k`, `capsys`; GitHub account, `gh auth login`, README in Markdown, `gh repo create --public --source=. --push`, `gh browse`, remotes, `origin`, tracking, `git push`, `git pull`, why not to amend pushed commits, never commit secrets.

**Hands on (the `life` package, v0.1.0):** `from life import Cell, step, generations, neighbours, normalise, period, PATTERNS, parse, shift, soup, render`. `step(live: set[Cell]) -> set[Cell]` is pure; `generations(live)` is an infinite iterator starting with `live` itself; `parse(picture)` reads `O`/`.` pictures; `soup(width, height, density=0.3, seed=None)`; `render(live, width, height) -> str`, two characters per cell. `life.cli:main(argv=None)`. Used again, unchanged, by P13 (Life in Pixels).

**Owes:** P13 a Pygame front end on this core, and an RLE parser (floated in Invent). P19 bits. P20 secrets management. P8 "no shared hidden state — hold that thought".

## Project 7 · Fractal Factory

**Taught:** `int`: unbounded, `_` separators, `int(s, 16)`, `hex`, `bin`; `float`: binary doubles, `0.1 + 0.2`, the three rules (no `==`, use `math.isclose`/`approx`; don't accumulate, multiply; never for money), `sum` compensates since 3.12 but loops don't, int→float loses precision, `inf`/`nan`/`math.isnan`; `round` half-to-even and `round(2.675, 2)`; `Decimal` (from strings), `Fraction`; `complex`: `j`, `.real`/`.imag`, `abs`, `conjugate`, as a point on a plane; `math.sin`, `math.hypot`, `math.ceil`; Pillow basics (`Image.new`, `putpixel`, `putdata`, `save`, modes `"L"`/`"RGB"`); **functions as arguments** and `Callable[[A], R]`; `type` aliases for function shapes (`Field`, `Palette`); **closures** and factories; higher-order functions that wrap functions (`cycled`, `black_inside`, `timed`); **LEGB**, `nonlocal`, shadowing built-ins (A001); **late binding** in loops (B023), shown as a picture, cured by a factory; `lambda`, and `key=` for `sorted`/`max`; `*args`, `**kwargs`, bare `*` (keyword-only), bare `/` (named), `f(*xs)`, `f(**d)` (paying P5's `State(**data)`); the pass-everything-through wrapper and "this is what a decorator is" (P16); `functools.partial` (paying P2's `onkey` problem), `functools.cache` and memoisation (needs hashable args); `argparse` `type=` with any callable incl. `complex` and a custom converter raising `ArgumentTypeError`; `--opt=-value` for values starting with a dash; test spies via closures. Tools: `time.perf_counter`, `python -m timeit -s`, `cProfile` (`ncalls`/`tottime`/`cumtime`), "measure, find the surprise, fix it, measure again"; the folklore escape-test is 4× *slower* in CPython ("what costs is the number of interpreter steps"); Knuth; algorithmic wins beat micro-optimisation (Extend 2: ~4×); `git tag -a`, `git tag`, `git push --tags`, `git switch --detach`, `git diff v1.0`.

**Owes:** P8 `global` ("you won't need it until Project 8"); mutation vs re-tying in closures "comes up again in Project 8". P9 classes ("data with behaviour attached, kept private, in miniature"). P16 decorators and `@` syntax; `functools.wraps`. P27 tags become releases. NumPy is only ever an Invent challenge here; P11/P13 may use it as a stretch.

**End of Part 1.** The chapter closes with a Part 1 summary and the remark that classes have been deliberately held back.

## Project 8 · Mode 2 Sketchpad

**Assumes (so these must be taught by P7):** packages, `src` layout, editable installs and `[project.scripts]` (P5/P6); `_private` naming convention (P6); `__init__.py` re-exports (P6); `match` (P5); `*` unpacking in calls (P7); `zip` and comprehensions (P3); slices and `del` on a slice (P3); type hints incl. `X | None`, `tuple[int, int]` (P3 onwards); pytest, fixtures-as-parameters, `parametrize`, `pytest.raises(match=)`, `tmp_path` (P3–P6); `conftest.py` (introduced *here*); aliasing bug (P4); profiling, "measure before you optimise" (P7); the debugger basics (P2); dataclasses exist (P5) though unused here.

**Taught:** pygame-ce vs pygame (`IS_CE`); the game loop: events, update, draw, `flip`, `Clock.tick`; `Surface`; `pygame.draw.*`; top-left origin; off-screen surfaces and `blit`; palettised (8-bit) surfaces, `set_palette`, `get_at_mapped`, `set_palette_at`; flags with `|`; `SystemExit`; `global`, and the rule that assignment makes a name local (`UnboundLocalError`); mutation needs no `global`; pure functions vs stateful ones; `divmod`; `match` on ints; tuple shuffle `_previous, _cursor = _cursor, (x, y)`; `type X = ...` alias, first sight; `zip(strict=True)`; `__all__`; SDL dummy driver for headless tests; test interdependence via shared state; the update–clear–draw–vsync frame shape; `event.unicode`; `"" in "abc"` gotcha. Tools: `launch.json`, conditional breakpoints, logpoints, Debug Console.

**Hands on (the `beeb` package, v0.1.0):** `mode(n)`, `gcol(action, colour)`, `clg()`, `move(x, y)`, `draw(x, y)`, `plot(k, x, y)` (k: 0–7 lines, 64–71 points, 80–87 triangles), `point(x, y)`, `vsync()`, `inkey()`, `mouse()` → `(x, y, (left, middle, right))`, `screenshot(path)`, `WIDTH`, `HEIGHT`; `beeb.screen.to_pixel`/`to_units`; `uv run beeb` shows a test card. Solutions add `colour(logical, physical)`, flashing colours, `fill`, `circle`. Used again by P11 (adds sound) and P28 (the capstone).

**Reconciled with Part 1** after Part 1 was written (type alias now a reminder; writing your own `@pytest.fixture` and `conftest.py` sharing explained).

**Owes:** P9 classes as the cure for module-level state (the chapter's five complaints: growing `global` lists, `None` checks, interdependent tests, action at a distance, only one screen). P17 type aliases in depth. P19 bit operations in depth.

## Project 9 · Snake

**Taught:** classes as the cure for P8's five complaints (table in Stage 4); methods on a dataclass as the bridge; `class`, `CapWords`, docstring; `__init__` as initialiser, "calling the class"; attributes come into being by assignment, all in `__init__` by convention; `self` is an ordinary first parameter, `obj.m()` is `Class.m(obj)`; no `new`, no `private`, no implicit `this`; class attributes for constants; attribute lookup (instance, then class) vs assignment (always the instance); **the mutable class attribute trap** (RUF012), linked to P4's mutable default and to `default_factory`; enums with methods; `auto()`; quoted forward reference in a hint, and that 3.14 no longer needs it; `collections.deque`: `appendleft`/`popleft`/`pop`, `maxlen`, why not `list.insert(0, …)`; input buffering with a bounded queue; **composition** ("has a"), objects asking objects; injecting `random.Random` for determinism; `rng or random.Random()` idiom; **state machine** with an `Enum`, `is` for enum members; **time-based updates** (`update(seconds)`, accumulator, catching up), `clock.tick()` returning milliseconds; View reads Game, Game knows nothing of View; `pygame.font.Font(None, size)`, `render(text, False, colour)`; `match` on dotted enum constants; testable `handle(event, game) -> bool`, building `pygame.event.Event` by hand; fixtures that return objects; **when not to write a class** (three-way test: class / function / dataclass). Git: `git stash`, `stash pop`, `stash list`, `-u`, "WIP commit on a branch is safer"; `.gitignore` patterns (`*`, leading `/`, trailing `/`, `!`), `git check-ignore -v`, `git rm --cached` for already-tracked files, global `core.excludesFile`.

**Hands on (the `snake` package):** `snake.model`: `Direction` (values are `(dx, dy)`, `.opposite()`), `Snake(head, heading)` with `.body` (deque, head first), `.head()`, `.turn()`, `.grow()`, `.advance()`, `.bites_itself()`; `State`; `Game(columns, rows, rng)` with `.state`, `.score`, `.best`, `.snake`, `.food`, `.start()`, `.toggle_pause()`, `.turn()`, `.update(seconds)`, `.step()`. P22 will have it post `score` to the high-score server.

**Owes:** P10 properties ("will turn `snake.head()` into `snake.head`"), class methods, `__repr__`, levels from text files, `frozen=True`. P12 dunder methods for `+`, `==`, `len`. P15 inheritance ("a good deal later than you might expect, and for a reason"). P22 posting scores. Invent 2 floats "make `beeb` a class" — optional, not relied on later.

**Layout of Part 2 games, established here:** `model.py` (no Pygame; rules and state), `view.py` (all drawing; low-res canvas scaled up), `app.py` (window, loop, `handle()`); `tests/conftest.py` sets SDL's dummy drivers.

## Project 10 · Breakout

**Taught:** `pygame.FRect`/`Rect`: construction, the virtual attributes (`right`, `centerx`, `midtop`, …) that can be assigned to, `colliderect`, `clamp_ip` (and the `_ip` convention), `inflate`, `move`, `copy`; Pygame's import banner; `@dataclass(frozen=True)`, `FrozenInstanceError`, `replace` for variants, frozen means hashable, "sharing is safe when the shared thing can't change" as a design; **`@classmethod`** and `cls`, alternative constructors named `from_…` (with stdlib examples), one fundamental `__init__` plus conveniences, why `cls(…)` not `Level(…)`; `@staticmethod` (named, discouraged); `path.stem`; **data files inside a package** and `importlib.resources.files(…)`; `sorted(key=lambda …)`; **`__repr__`** vs `str`, who each is for, convention (looks like the constructor, or is informative), dataclasses write their own, with a real anecdote of a repr diagnosing a failing test; **`@property`**: read-only computed attributes, setters with validation, `x.prop *= k` works, private backing attribute `_name`, "don't write getters and setters; start with an attribute, promote later", "a property should be cheap and unsurprising"; `levels if levels is not None else …` vs `or`; `copy.deepcopy` in anger; axis-separated movement to find which side was hit; clamping `seconds` against tunnelling; bat-offset → angle; **events vs key state** (`pygame.key.get_pressed()`), `int(True)`. Tools: refactoring in VS Code: Extract method (Ctrl+.), Rename symbol (F2), Go to Definition (F12), Find All References (Shift+F12), go back; "refactoring without tests is editing and hoping"; a refactoring gets its own commit.

**Hands on (the `breakout` package):** `Settings` (frozen), `Brick`, `Level` (`from_text`, `from_file`, `built_in`, `.cleared`, `.hit_by(rect)`), `Bat`, `Ball` (`.speed` property, `.aim(offset)`), `State`, `Game(levels, settings)` with `.score`, `.best`, `.lives`, `.level`, `.serve()`, `.update(seconds, steer)`. P22 will have it post `score`.

**Owes:** P11 sound ("make the sounds out of arithmetic"; Invent 1 floats adding blips here). P12 the rest of the dunder methods. P15 subclassing a `Level` (the reason for `cls(…)`). P27 package data travels with the built package. Tweak 3 asks the reader to make Snake's `head` a property; later chapters must not depend on either spelling.

**Infrastructure:** `scripts/fill_listings.py` (authoring aid: `#@ from: X | to: Y` directives inside a marked listing are filled from the real file; the filled chapter is committed and still verified by `check_docs.py`). `check_docs.py` hides Pygame's banner and uses SDL's dummy drivers; the root dev group now includes `pygame-ce` so chapter REPL sessions can import it. Repo-level tests skip on their own project's package, not on `pygame`.

## Project 11 · SOUND & ENVELOPE

**Taught:** what a sample is; sample rate, 16-bit range; `bytes` (immutable, indexing gives ints, `b'…'` display, `.hex(" ")`), `bytearray` (named), `int.to_bytes`/`from_bytes`, **endianness**; `array("h")`: `itemsize`, `tobytes()`, `array("h", some_bytes)`, `OverflowError` (Python's ints don't overflow, fixed-size ones do), two's complement in a sentence; the `wave` module as a context manager; `itertools.count`, `itertools.cycle` (Predict); **generators as a signal chain**: endless oscillators (`square`, `sine`, `noise`), a finite ADSR envelope (`levels`), `zip` deliberately without `strict`; ADSR; BBC pitch numbers (`261.63 * 2 ** ((pitch - 53) / 48)`); **caching and mutability**: `cache` needs hashable args (so `Envelope` is frozen) and should return immutable results (so `note` returns `bytes`); **class inside, module functions outside** (one lazily made `_speaker`, like the `random` module); an honest single `global`; `pygame.mixer`: `init`, `get_init`, `quit`, `allowedchanges=0`, `Channel`, `Sound(buffer=…)`, `get_busy`, `get_length`, and the who-opened-the-device-first gotcha; a module and a function can't share a name in a package (`speaker.py`, not `sound.py`); version bump to 0.2.0, minor = new features; **a model that reports events** (`game.events: list[str]`), event queue pattern, why the model mustn't call `beeb.sound`; `f(*SOUNDS[event])`; phase accumulation for a changing frequency (type-in). Tools: `uv add --editable ../beeb`, `dependencies` says what and `[tool.uv.sources]` says where, editable path deps; workspaces, Git dependencies and publishing, in one Under the bonnet box; merging a feature branch and tagging `v0.2.0`.

**Hands on (the `beeb` package, v0.2.0):** everything from v0.1.0, plus `beeb.sound(channel, amplitude, pitch, duration)` (channel 0 noise, 1–3 tones; amplitude 0…−15 or envelope 1–4; pitch in quarter-semitones, 53 = middle C; duration in 1/20 s; a new note on a channel replaces the old) and `beeb.envelope(number, attack, decay, sustain, release)`; `beeb.synth`: `RATE`, `LOUDEST`, `Envelope` (frozen), `PLAIN`, `frequency_of`, `square`, `sine`, `noise`, `levels`, `samples`, `note` (cached, returns `bytes`); `beeb.speaker.Speaker`. Used again by P28. Breakout (in the reader's own project) now has `game.events` and depends on `beeb` by path.

**Owes:** P27 version numbers in full; publishing so that `uv add beeb` works for everybody. P28 the capstone's `SOUND`/`ENVELOPE` statements call these functions.

**Repo layout note (P11):** `projects/11-sound-and-envelope/` is beeb v0.2.0, and holds a nested project, `noisy-breakout/`, derived from Project 10 with a path dependency on `..`. `check_all.py` checks nested projects; both pyprojects carry a `[tool.ruff]` table for this repo's sake only.

## Project 12 · Asteroids

**Taught:** **the data model**: operators are method calls (`__add__`, `__sub__`, `__mul__`, `__truediv__`, `__floordiv__` named), operators return new objects and leave operands alone, reflected methods (`__rmul__`) and the `NotImplemented` dance, `isinstance(x, (int, float))`, no `__iadd__` means `a += b` is `a = a + b`; `__eq__` costs you `__hash__`, equal objects must hash equal, only immutable objects should be hashable, `@dataclass(frozen=True)` writes both; value types; `__neg__`, `__abs__`, `__bool__` (falls back to `__len__`), `__iter__` as a generator method (so unpacking, `tuple()`, `max()` work); a table of syntax → dunder (`__len__`, `__getitem__`, `__contains__`, `__call__`, `__lt__`, `__str__`…); "overload only when the meaning is obvious"; `sum(vectors, Vector())`; `pygame.Vector2` is mutable (box); **duck typing** named at last, and **`typing.Protocol`** (attribute protocols and method protocols with `...`), structural typing, nothing inherits from a protocol, `isinstance` needs `@runtime_checkable` (named only), stdlib hints like `Iterator`/`Callable` are protocols; similar classes with **no base class** ("hold that thought until Project 15"); thrust vs velocity, drag, everything × `seconds`; an endless generator as a supply of levels; **the walrus `:=`**; looping over `list(xs)` so as to remove from `xs`; module-level tables not class-level dicts (RUF012). Tools: Pylance `typeCheckingMode: "standard"` with two worked squiggles (protocol mismatch, `Bullet | None`); **a real merge conflict**: markers, Accept Current/Incoming/Both, the Merge Editor, `git merge --abort`, test before committing the merge, `git commit` with no `-m`, merge commits with two parents, `git log --graph`; "merge often, keep branches short".

**Hands on (the `asteroids` package):** `asteroids.vector.Vector` (frozen; `+ - * /`, unary `-`, `abs`, `bool`, iterable, `from_polar(length, degrees)` measured clockwise from up, `rotated`, `wrapped`); `asteroids.model`: `Controls`, `Body` and `touching`, `Zone`, `Ship`, `Bullet`, `Rock`, `waves(rng)`, `State`, `Game(rng)` with `.events` (`"fire"`, `"bang"`, `"crash"`), `.score`, `.best`. Depends on `beeb` by path for sound. P22 will have it post `score`.

**Owes:** P13 Life in Pixels, reusing P6's core unchanged. P14 `__matmul__` (`@`) and `__slots__`. P15 inheritance and ABCs, and when a base class *is* right. P17 Pylance `strict`. P18 `__enter__`/`__exit__`. P24 descriptors (`__get__`/`__set__`).

## Project 13 · Life in Pixels

**Taught:** reusing your own package unchanged (`uv add --editable ../life`; hyphenated project, underscored package); two coordinate systems and a `Camera` dataclass between them (`cell_at`, `pixel_of`, `pan`, `zoom_about` a point, clamping with `max(min())`); **`math.floor` vs `int()`** for negatives (and `//`); testing a *property* rather than numbers; `set.discard` vs `remove`; copying a caller's set; a clock with a catch-up limit and a time budget (`time.perf_counter`); **regular expressions**: raw strings, `\d`, `*`, `+`, `.`, `[…]`, groups, `re.compile`, `findall`, `sub`, `search` (named), a function as the replacement, a `lambda` doing it, "regex finds the pieces, Python decides what they mean", deleting all tokens to validate; an exception of your own as a kind of `ValueError` (exceptions are a family tree); one `App` class holding state, `handle(event) -> bool` tested with made-up events; `match` on `event.type` with guards, two cases for one type, `case _ if`; tri-state `True/False/None` and `is not None`; `cells_between` for gap-free strokes; `pygame.DROPFILE`, `MOUSEWHEEL`, `RESIZABLE`; `try/except/else` around file loading with a tuple of exceptions; timing two costs separately; **culling**; `window.fill(colour, rect)`; measuring three versions of `step` and **leaving it alone**; comparing two views' pictures byte for byte. Tools: **`git bisect`** by hand and `git bisect run` with a test, `git switch --detach`, `uv run --no-project --with pytest`, why small working commits matter, don't revert blindly, the test is the search engine.

**Hands on (the `pixel_life` package):** `pixel_life.camera.Camera`; `pixel_life.rle.parse(text) -> set[Cell]` and `RLEError`; `pixel_life.simulation.Simulation` (`live`, `generation`, `running`, `speed`, `step`, `update`, `paint`, `load`, `faster`); `pixel_life.view.View`; `pixel_life.app.App`. `solutions/rle_writer.py` has `to_rle` (uses `itertools.groupby`, which is named in the challenge, not taught).

**Owes:** P14 the third dimension, matrices and `@` (`__matmul__`), `__slots__`, `tomllib`. NumPy is named only, in an Invent challenge.

## Project 14 · Wireframe

**Taught:** right-handed axes (x right, y up, z towards you); `Vec3` as a frozen slotted dataclass, `math.hypot` with three arguments, **dot** and **cross** products as named methods and what each is for; **`__slots__`**: `__dict__` shown, fixed places, `@dataclass(slots=True)` writes it (and builds a second class), hand-written slots can't have class-level defaults, no `cached_property`; **measured**: 104 → 64 bytes an object with `tracemalloc`, no speed difference on 3.14 ("the advice has outlived the fact"), when it's worth it (very many small objects; typos in attribute names as errors); **matrices**: row·vector, matrix·matrix as rows·columns, `zip(*rows)` to transpose, two comprehensions and a `zip`, `strict=True` catching misfitting shapes; **`__matmul__`** and **`__rmatmul__`** (the vector answers for `matrix @ vector`, so `matrix.py` needn't import `vec3.py`); `a @ b != b @ a`, read chains right to left; an identity constant shared because immutable; rotation matrices, transpose of a rotation is its inverse (tested), rotations keep lengths (tested); orientation as one accumulated matrix, `step @ orientation` (screen axes) vs `orientation @ step` (ship axes); **TOML** written (tables, `[[arrays of tables]]`, dates, comments; TOML vs JSON) and read with **`tomllib`** (`loads`/`load`, no dump, `tomli-w` named); **`match` on nested data**: type patterns `int()`, `str(name)`, `int() | float() as x`, mapping patterns, extra keys ignored, `case {}` matches everything, `**others` with a guard; `raise … from` reused; adding a ship by adding a file (`importlib.resources` again); `assert old in TEXT` guarding replace-based tests; **perspective** by dividing by distance; **normals**, anticlockwise winding, **back-face culling** with perspective-correct eye vector, flat shading as a dot product with a lamp, `Color.lerp`, `pygame.draw.polygon(width=)`; convex-only limitation (painter's algorithm named); a **bug in a data file** handled as reproduce → failing test → fix. Tools: **project snippets** (`.vscode/*.code-snippets`, scope, prefix, body, tab stops, choices, mirrored stops, `$0`) and **tasks** (`tasks.json`, labels, shell commands, `dependsOn` in sequence, default build and test tasks, `problemMatcher: []`, why not `&&`). `itertools.combinations` appears in the type-in only.

**Hands on (the `wireframe` package):** `wireframe.vec3.Vec3` (`+ - *`, `abs`, iterable, `dot`, `cross`, `__rmatmul__`); `wireframe.matrix`: `Matrix(rows)` with `@`, `.columns`, `.transposed()`, `IDENTITY`, `rotation_x/y/z(degrees)`; `wireframe.model`: `Face`, `Ship` (`.radius`), `ModelError`, `parse`, `load`, `hangar`; `wireframe.projection`: `project`, `normal`, `faces_the_eye`, `brightness`; `wireframe.view.Style`/`View`; `wireframe.app.App`. `solutions/bughunt/checks.py` has `inside_out(ship)`. Ship files are TOML: `name`, `points`, `[[faces]]` with `points` and optional `colour`. P28's capstone may borrow `Matrix`/`Vec3` for a 3D demo.

**Owes:** P15 inheritance and ABCs ("a family of classes with enough in common to deserve a parent"), the command pattern, logging, conditional breakpoints (already met in P8; P15 deepens). P17 `match` on dataclasses (class patterns with attributes) and `@overload` if wanted.

## Project 15 · Sprite Editor

**Taught:** designing a **text file format** (header line, `.` and digits, comments; diff-able, hand-editable, testable in strings) vs PNG/JSON; sparse `dict[Cell, int]` storage; **container methods** `__getitem__`/`__setitem__`/`__contains__`, commas in brackets make a tuple, slices arrive as `slice` objects, `IndexError` for out of range; **`cls(…)` paid off** with a subclass, and **`typing.Self`**; **the command pattern** (do/undo objects, two stacks, `undone.clear()`, three ways to undo: remember, repeat, reverse); **inheritance**: `class Child(Parent)`, is-a, overriding vs extending, **`super()`** (and the forgotten `super().__init__()` gotcha), a parent's method calling a child's (`self.kind()`), **template method** (`ShapeTool.cells`, `Button.draw_face`), **ABCs** (`ABC`, `@abstractmethod`, error at instantiation naming the method, abstract classes with concrete members), `__mro__`, everything ends in `object`, **polymorphism** named; **when not to**: one class with different data (`Paint`), the five-row table (duck typing / Protocol / ABC / data / composition), "is a kind of" taken seriously, shallow trees; iterative flood fill (why not recursion); **callbacks**: bound methods, `partial` for arguments, late-binding lambda recalled (B023), a list's `append` as a spy; an event router (first button containing the pointer, else the canvas); `dict | dict`; `mod & FLAGS` read as "is it held?" (bits proper owed to P19); "changed since save" by comparing history lists. Tools: **`logging`**: `getLogger(__name__)` per module, five levels, `%s` lazy arguments (and the mutable-argument gotcha met for real with `caplog`), `basicConfig` only in `main`, `-v`/`-vv` with `action="count"`, `--log-file`, "libraries log, applications configure", **`log.exception`** in the one legitimate catch-all at the top of the event loop, **`caplog`**; debugger: a `"module"` launch configuration with `args` (needs the `__name__` guard for `python -m`), the Call Stack through callbacks, Watch, conditional breakpoints again, **hit counts**, **Raised/Uncaught Exceptions**.

**Hands on (the `sprite_editor` package):** `sprite_editor.sprite`: `Sprite(width, height, pixels)` with `sprite[x, y]`, `in`, `to_text`, `from_text`, `save`, `load`, `SpriteError`, `Cell`, `Colour` (0–7 BBC colours, `None` see-through); **the `.sprite` text format** (`sprite W H`, then rows of `.` and `0`–`7`, `#` comments), which **P28's capstone loads for its hardware sprites**; `sprite_editor.commands`: `Command` (ABC), `Paint`, `Flip`, `Shift`, `History`; `sprite_editor.shapes`: `cells_between`, `box`, `flood`; `sprite_editor.tools`: `Tool` (ABC), `Pencil`, `Eraser`, `ShapeTool` (ABC), `Line`, `Box`, `Bucket`, `Picker`; `sprite_editor.widgets`: `Button`, `Swatch`, `Canvas`, `PALETTE`. `examples/invader.sprite` and `examples/rocket.sprite`. `solutions/more_tools.py`: `FilledBox`, `Ellipse`, `Turn`.

**Owes:** the Pyxel side quest next. P16 decorators, `functools.wraps`, a renderer `Protocol`, pull requests. P19 bits and `&`/`|` properly. P24 Textual, where subclassing widgets is the norm ("you'll see it again with Textual in Part 5"). P28 sprites come back as hardware sprites.

## Side quest · Pyxel

**Taught (optional; nothing later may depend on it):** what a fantasy console is; **framework vs library** ("a framework calls you"); `pyxel.run(update, draw)` as callbacks; module-with-a-screen design recognised from `turtle`/`beeb`; `btn` vs `btnp` = held vs pressed; frame-counted time vs measured time; **how to pick up an unfamiliar library** (quickstart, examples, reference, stubs/source; tutorials last); **`.pyi` stub files** (why `help()` is empty for a Rust-cored library while Pylance hover works); `str.maketrans`/`translate`; the P15 `.sprite` format loaded into Pyxel in four lines. No Predict, bug hunt or solutions, by design. Pyxel needs OpenGL and (on Linux) system SDL2, so CI runs the game against `tests/fakepyxel.py`; the real thing is exercised by `scripts/screenshots_p15x.py`, which needs a display.

**Hands on:** nothing. `projects/15x-pyxel-side-quest/meteors.py` is a flat script.

**Owes:** the bonus chapter after Part 4 (`pyxel app2html` was called "a preview"); the briefs chapter's Pyxel demake. Its closing link to Project 16 points at the roadmap until P16 exists.

## Project 16 · Logo

**Taught:** **decorators** (P7's promise): `@d` is `f = d(f)`, runs once at definition; registering decorators vs wrapping decorators; decorator factories (three layers); `inspect.signature` to count parameters; **`functools.wraps`** (name, docstring, `__wrapped__`, and why `inspect.signature` and the registry break without it); stacking order (bottom up); `autouse=True` fixtures; registration as an import side effect and `__init__.py` importing a module for it (in `__all__` to satisfy F401); trailing-underscore names (`if_`, `print_`); **tokeniser as a generator** with `re.VERBOSE`, named groups, `|` alternation order, `finditer`, `lastgroup`, `rf"""` strings, a lookbehind (Logo's minus-sign rule); a recursive `type` alias; **recursive descent** twice (brackets via a shared iterator; expressions via a grammar with one method per rule; precedence comes from which method calls which); evaluate-while-parsing vs building a tree (P17); a `Stream` with `peek`/`take`; **class patterns with keyword attributes** `Token(kind="number", text=text)`; **`ChainMap`** (`new_child`, `parents`, `maps`, writes go to the first map), **the call stack**, why recursion needs it, **dynamic vs lexical scope**; `try/finally` to pop a scope; **exceptions as control flow** (`Stop`, and `StopIteration` named); `raise … from` turning Python errors into `LogoError`; error messages as UI, tested by a parametrised list of wrong programs; a **`Protocol` with three implementations** in different files (`Recorder`, `SvgCanvas(Recorder)`, `PygameCanvas`), first taste of SVG; `TEXTINPUT` events, a line editor with history, multi-line input via `is_complete`; callbacks again (`say=`). Tools: **pull requests** with `gh pr create/view/diff/merge --squash --delete-branch`, `git push -u`, self-review, squash vs merge, `gh issue create`, `Fixes #n`. Bug-hunt lesson: shrink the example.

**Hands on (the `logo` package):** `logo.Interpreter(canvas, say=print)` with `.run(text)`, `.turtle`, `.variables` (ChainMap), `.procedures`; `logo.LogoError`; `logo.tokens.tokenise`/`Token`; `logo.reader.read`/`Stream`/`Item`; `logo.registry`: `COMMANDS`, `@command(*names)`, `@numeric`, `show`; `logo.turtle`: `Canvas` protocol (`line(start, end, colour)`, `clear()`), `Turtle`, `Recorder`, `SvgCanvas.save(path)`, `COLOURS`; `logo.app`: `PygameCanvas`, `App`, `is_complete`, `draw_to_svg`; CLI `logo [program] [--svg FILE]`. 31 command names. `solutions/extras.py`: `@traced`, `SETHEADING`, `XCOR`, `YCOR`, `SQRT`.

**Owes:** P17: parse once into a tree of dataclasses, walk it with `match`; precedence climbing; Pylance strict; CI (pull requests get their checks there: "next project, it's where your tests will be run for you"). P18: SVG properly. Part 4: Flask/FastAPI route decorators are registries. From here on, challenges are merged by PR.

## Project 17 · Tiny BASIC

**Taught:** parse once into a **syntax tree** vs re-reading text (measured: parsing a line costs ~6× running it); the tree as **frozen slotted dataclasses** with **union aliases** (`type Expression = …`), why a closed union and not a base class; **`match` with class patterns by position**, literal values inside patterns, `|` alternatives binding the same names; **`typing.assert_never`** and exhaustiveness; **precedence climbing** with a table, associativity as the `+ 1`, unary operators' levels, Python's own rules compared; statements parsed by a `match` on the first token, `IF … THEN … ELSE` taking the rest of the line, errors at entry time not run time; **the iterator protocol** (`__iter__`/`__next__`/`StopIteration`, iterators are used up) and a **generic class `Peekable[T]`**; **generic functions with bounds** to type a registering decorator (`def function[F: Callable[..., Value]]`); **`Literal`**; **`TYPE_CHECKING`** to break a circular import (and what a circular import is); the **`operator` module** and tables of functions; BBC truth values −1/0 and bitwise AND/OR/NOT; a **flat list of steps with a program counter**, `GOTO` as an assignment, stacks for FOR/REPEAT/GOSUB, `step()` so the machine can be driven a slice at a time; a `Console` `Protocol` with three implementations; **`@contextmanager`** (before/after the `yield`, `try/finally`, what `as` receives), `nullcontext`, `suppress` and `redirect_stdout` named; REPL named. Tools: **pyright/Pylance `strict`** via `[tool.pyright]` in `pyproject.toml`, `uv add --dev pyright`, the two real findings (`Generator[None]` for context managers; `operator.truediv` untyped), `# pyright: ignore[rule]` with the rule named; **coverage** with `pytest-cov` (`--cov`, `--cov-branch`, `term-missing`, reading `Missing`, error paths, `# pragma: no cover`, what coverage can't tell you, `--cov-fail-under`); **GitHub Actions** (workflow YAML, `on`, matrix of OS × Python, `uses` vs `run`, `uv sync --locked`, `gh run watch/list/view --log-failed`, required checks on PRs, "CI should run exactly what you run"); **`uv build`** (wheel and sdist) and **`uv tool install .`**. Bug-hunt lesson: associativity; "coverage counts lines, and bugs live in cases". **`TypedDict` was in the plan for here and is deferred to P22**, where JSON gives it a natural home.

**Hands on (the `tiny_basic` package, command `basic`):** `tiny_basic.Machine(console)` with `.enter(text)` (numbered line → stored; `RUN`/`LIST`/`NEW`/`SAVE`/`LOAD`; else immediate), `.store`, `.listing()`, `.new()`, `.save(path)`, `.load_file(path)`, `.load(immediate=())`, **`.step() -> bool`**, `.run()`, `.variables`, `.console`, `.write(text)`; `tiny_basic.Console` protocol (`write(text)`, `read(prompt) -> str`); `tiny_basic.BasicError` (`.message`, `.line`); `tiny_basic.registry`: **`@function(name)`** (values in, value out; arity from the signature) and **`@command(name)`** (first parameter is the machine): **this is how P28 adds `MOVE`, `DRAW`, `GCOL`, `SOUND`, sprites and `COLLIDE` without touching the parser**; `tiny_basic.values`: `Value`, `number`, `string`, `show`, `combine`, `TRUE`/`FALSE`; `tiny_basic.parser.parse(text)`, `Parser`, `PRECEDENCE`; `tiny_basic.nodes`; `tiny_basic.peekable.Peekable`. Statements: PRINT LET INPUT IF/THEN/ELSE GOTO GOSUB RETURN FOR/TO/STEP/NEXT REPEAT/UNTIL REM END, plus registered commands (CLS). Limits to remember for P28: `INPUT` blocks on `console.read`; loops can't start inside an `IF` branch; no arrays, PROCs or DATA (challenges).

**Owes:** P18 a context manager as a class (`__enter__`/`__exit__`), SVG properly, t-strings. P19 bits. P22 `TypedDict`. P27 publishing. P28 the capstone built on this package.

## Project 18 · SVG Plotter

**Requires Python 3.14** (t-strings); every earlier project is `>=3.13`. **Taught:** "a web page is a picture described in text"; **SVG** (XML elements and attributes, `viewBox`, `<path d="M… L…">`, `<text>`, `<g transform>`, y downwards); **building strings**: parts in a list and `join`, measured (`join` 0.4 ms, local `+=` 2.8 ms, `self.text +=` 786 ms for 100k pieces: CPython's in-place trick only works for locals), adjacent literals join, **format specifications** recapped (`.1f`, `8.2f`, `g`, `,`, `!r`) and measured (`:.1f` makes path data 35% of the size); **escaping** (`html.escape`, `quote=`), **injection** named (XSS, SQL), "a safe way that depends on never forgetting isn't safe"; **t-strings** (PEP 750): `Template`, `.strings`, `.interpolations`, iterating parts, `Interpolation(value, expression, conversion, format_spec)` matched by position, `convert`, `format`; writing `render`; a **marker subclass of `str`** (`Safe`) to avoid double escaping; nested templates and lists; forward references need no quotes in 3.14, and a `type` statement may precede what it names; keeping a **tree of elements** and rendering with `match` to Templates (P17's lesson reapplied); nested class patterns; **a context manager as a class**: `__enter__` returning `Self`, `__exit__(kind, error, traceback)` and its type hints, saving only on success, **returning `True` swallows the exception** (the bug hunt), class vs `@contextmanager` and when to use each; a stack of open groups; CSS stroke-dash animation with `pathLength="1"`, timing by stroke length; **testing markup by parsing it** with `xml.etree.ElementTree` (namespaces in braces, round-trip of awkward characters); `python -m http.server`, `file://` vs `http://`, status codes 200/404, `ThreadingHTTPServer` + `partial(SimpleHTTPRequestHandler, directory=…)`, `127.0.0.1`, port 0, servers as context managers, testing a server in a thread with `urllib.request`; `webbrowser.open`, `Path.as_uri()`; `contextlib.chdir` (in the repo's script only). Tools: **Live Preview** (flagged unverified). Bug-hunt lesson: "it never crashes" means it hides its errors.

**Hands on (the `plotter` package):** `plotter.Plot(path, width=1280, height=1024, paper="black", title="", seconds=0.0)` as a context manager, with `move`, `draw`, `pen(colour, width=None)`, `label(x, y, words, size=32)`, `turned(degrees, about=)`, `svg()`, `save()`; origin bottom-left, BBC style; `plotter.render(template) -> Safe` and `plotter.Safe`, **which P19+ can reuse for HTML**; `plotter.gallery`: `page(folder)`, `server_for(folder, port)`, command `gallery [folder] [--port] [--no-browser]`. `solutions/chart.py` has `chart(plot, function, x_range, y_range, name)`.

**Repo notes:** `check_docs.py` now runs each chapter's REPL sessions inside a temporary directory, and puts the project's `stages/` on the import path as well as `src/`. The chapter's pictures are SVG files in `docs/assets/`, made by `scripts/screenshots_p18.py`. `pictures/` is git-ignored.

**Owes:** P19 HTML proper, Jinja2 (the industrial-strength version of `render`), CSS, **bits** (owed since P15 and P17), GitHub Pages. P20 Flask: real servers, routes as decorator registries (P16's promise).

## Project 19 · PyFax

**Taught:** **bits** at last (owed since P15/P17): binary, `bin`, `0b`, `:06b`, `&` `|` `^` `~` `<<` `>>`, masks, set/clear/flip/test, the two ways of testing a bit and what `dots & mask` really returns (the bug hunt), bit fields and flags, **`IntFlag`** and **`IntEnum`**, two's complement and why `~5 == -6` and why BBC TRUE was −1, colours as three bits (why there were eight), comparison vs bitwise precedence in Python vs C; **round-trip tests** over all 64 values with `parametrize(range(64))`; `[x] * n` sharing: safe for frozen cells, a trap for rows; closures as local helpers; ceiling division `-(-n // 3)`; `dict.fromkeys` to de-duplicate in order; **`textwrap.wrap`**; `\b` word boundaries; **static site generators**; **HTML** basics (`head`/`body`, `main`, `div`, `span`, `a`, `p`, `data-` attributes) and **CSS** basics (selectors, classes, properties, **CSS grid**, `fr`, `vw`/`vh`, `min()`, `display: contents`, multiple backgrounds); **Jinja2**: `{{ }}` vs `{% %}`, `for`/`if`, **template inheritance** (`extends`/`block`), **filters** (built-in and your own), whitespace control, `Environment`, `PackageLoader`, **`select_autoescape()`** ("never make an HTML environment without it"; Flask does it for you); **generating CSS from data** ("when something follows a rule, generate it"); a dozen lines of JavaScript, explained once; **`html.parser.HTMLParser`** by subclassing (callbacks via inheritance) and a **link checker** test; `scope="module"` fixtures; aware local dates with `datetime.now().astimezone()` (DTZ rules; `datetime` proper is owed to P23). Tools: **GitHub Pages** via an Actions workflow (`configure-pages`, `upload-pages-artifact`, `deploy-pages`, `permissions`, tests before build), **relative links**; flagged unverified because this repo is private.

**Hands on (the `pyfax` package; P20 and P24 reuse `mosaic` and `page`):** `pyfax.Colour` (IntEnum 0–7, `.css`), `pyfax.Cell(text, ink, paper, dots, link)` (frozen), `pyfax.Page(number, title, section, before, after, rows)` with `write(row, column, text, ink, paper, link=)`, `picture(row, column, bitmap, ink)`, `fill(row, paper)`, `text()`; `COLUMNS`, `ROWS`; `pyfax.mosaic`: `Dot`, `pack(bitmap)`, `unpack(dots)`, `stylesheet()`; `pyfax.font.banner(words)`; `pyfax.content`: `Article`, `ContentError`, `load`, `load_all`, `lines_of`, `frame`, `lay_out`, `index`, `neighbours`, `pages(folder, today)`; `pyfax.build`: `classes(cell)`, `environment()`, `stylesheet()`, `write_site(pages, out)`, command `pyfax [content] [--out site]`; templates `base.html`/`page.html`, `static/style.css`, `static/pyfax.js`. Articles are TOML: `number`, `title`, `section`, `body` (`{colour}` lines), optional `picture`. `solutions/sprites.py`: `sprite_cells`, `stamp`.

**Repo note:** the chapter's pictures are drawn from the page model by `scripts/screenshots_p19.py` (a reference renderer with pygame), since there's no browser in CI. The real site was later checked in headless Chrome with `scripts/browser.py` (grid, colours, graphics characters and typed page numbers all work).

**Owes:** P20 a server (Flask), sqlite3, live data, forms, secrets; "Flask switches autoescape on for you". P23 `datetime` properly. P24 the Textual viewer of these pages.

## Project 20 · PyFax Live

**Taught:** what changes when a program stays running and talks to strangers; **Flask**: routes as registering decorators (P16's "decorators in the wild"), converters (`<int:number>`), views, `--debug` (reloader + in-browser debugger, and why never in public), the development-server warning, **the application factory** (`create_app`, why not a global `app`), **blueprints**, `current_app`, `app.extensions` for per-application state, `render_template`, `abort`, `app_errorhandler`, returning `(body, status)`, `Response` with a mimetype, **`url_for`**, `ChoiceLoader` to reuse another package's templates, Flask's autoescape-by-default; **`py.typed`** (a package must ship it for its hints to count) and choosing `standard` over `strict` for glue code; **`httpx`**: `Client`, `params=`, **time-outs** ("every network call needs one"), `raise_for_status` (a 503 is a reply, not an exception), `HTTPError`; APIs, query strings, JSON checked with a nested **`match`**; failure as a page not a crash (`Forecast | None`); a **generic `TimedCache[K, V]`** with an injectable clock, `time.monotonic`, **stale-on-error**, `exc_info=True`; **dependency injection** named, **`httpx.MockTransport`** to fake a service, a saved real reply as a fixture; **`sqlite3`**: tables, `PRIMARY KEY`, `NOT NULL`, `IF NOT EXISTS`, `INSERT`/`SELECT … ORDER BY … LIMIT`/`DELETE`, **`?` placeholders**, **transactions with `with db:`** (and the gotcha that `with` ends a transaction rather than beginning one), `sqlite3.Row`, `executescript`, `:memory:`, `python -m sqlite3`; Flask's **`g`** and `teardown_appcontext`, a connection per request, application contexts in tests; **forms**: GET vs POST, `request.form`, **server-side checks** (HTML `maxlength` is a courtesy), handing the form back with its contents and a 400, **post-redirect-get**, **`flash`** and signed session cookies; **configuration**: defaults < `from_prefixed_env` < arguments, per-OS tabs for setting variables; **secrets**: `SECRET_KEY`, never in Git, `secrets.token_hex`; chained pytest fixtures, the **test client**; debugging with `launch.json` (`module: flask`, `--no-debugger --no-reload`, `jinja: true`). Bug hunt: **SQL injection** via an f-string (O'Brien crashes it; `' OR '1'='1` reads everything); Ruff's S608 isn't on by default.

**Hands on (the `pyfax_live` package):** `create_app(config=None)`; settings `DATABASE`, `CONTENT`, `PLACE` (name/latitude/longitude), `WEATHER_SECONDS`, `SECRET_KEY`, `HTTP_CLIENT`, all overridable as `PYFAX_*` environment variables; routes `/`, `/<int>.html`, `/599.html` (GET+POST), `/style.css`, `/pyfax.js`; `pyfax_live.cache.TimedCache`; `pyfax_live.weather`: `Place`, `Forecast`, `Day`, `fetch`, `parse`, `describe`, `page`; `pyfax_live.db`: `get_db`, `close_db`, `init_db`, `add_letter`, `latest_letters`, `Letter`; `pyfax_live.letters`: `check`, `page`. Changes made to P19 for this: `py.typed`, `{% block head %}` and `{% block scripts %}` in `base.html`, and `pages(folder, today, others=())`.

**Repo notes:** Flask and httpx are in the root dev group so that `check_docs.py` can run this chapter's Predicts. Pictures come from `scripts/screenshots_p20.py`, which imports the reference renderer from `screenshots_p19.py`.

**Owes:** P21 sessions properly, htmx, `.env` files, CSRF and other web security basics, rebase. P22 FastAPI, pydantic, `TypedDict` (from P17), `async def`. P23 `datetime`. Deployment is only pointed at (Invent 1).

## Project 21 · Adventure Online

**Taught:** P5's promise kept: a pure `respond(state, text)` behind a web page, engine unchanged (only `py.typed` added); HTTP is **stateless**; state on the server vs state in the browser; **cookies** (what they are, ~4 KB, sent with every request to the site whoever caused it); Flask's **signed session** ("signed, and it isn't secret"), base64, `asdict`/`State(**…)` round trip, coping with an out-of-date session; `url_for('static', …)`; Jinja `{% include %}`, `{% with %}`, `for … else`, underscore-named partials; **htmx** 2.0.10, vendored: `hx-post`, `hx-target`, `hx-swap="beforeend scroll:#id:bottom"`, the `HX-Request` header, **partial responses**, **out-of-band swaps** (`hx-swap-oob`) for the status and for emptying the prompt, **progressive enhancement** (one view, page or piece; works with no JavaScript, tested); "tests of a server tell you about the server" (the `scroll:bottom` bug found only in a real browser); **web security basics**: **XSS** (escaped echo), **CSRF** (what it is, a per-session token in a hidden field, 403), **`secrets.compare_digest`** and timing, **GET never changes anything** (405s tested), cookie flags `HttpOnly`/`SameSite=Lax`/`Secure`, a forged cookie is ignored, **Content-Security-Policy `default-src 'self'`** and what it costs (no inline script or style; htmx `allowEval: false`), `X-Content-Type-Options`, `MAX_CONTENT_LENGTH` and "put a limit on everything a stranger can send"; what wasn't covered (HTTPS, passwords, rate limits) and OWASP. Tools: **`.env`** with python-dotenv (loaded by `flask run`), `.env.example` committed and `.env` ignored, `git check-ignore -v`, "if a secret is committed, change it"; **`git rebase`**: replaying commits, new hashes, never rebase shared commits, `--force-with-lease`, conflicts with `--continue`/`--abort`, `git pull --rebase`, `rebase -i` described, merge vs rebase. Bug hunt: **module-level state in a server is shared by every visitor** (found with two test clients).

**Hands on (the `cupboard_web` package):** `create_app(config=None)` (settings as `CUPBOARD_*` environment variables); routes `/` (GET), `/command` (POST; page or piece), `/restart` (POST); `cupboard_web.game`: `current_state`, `keep`, `token`, `token_matches`, `LONGEST_COMMAND`; templates `base.html`, `game.html`, `_turn.html`, `_status.html`, `_prompt.html`; `static/htmx.min.js` (0BSD), `style.css`, `icon.svg`. `solutions/transcripts.py`: a server-side transcript store.

**Repo notes:** `scripts/browser.py` drives a headless Chrome over the DevTools protocol (needs Chrome and `websockets`; an authoring tool, not part of CI). It verified this chapter's htmx behaviour and took `p21-game.png`. `check_docs.py` now also puts a project's path dependencies (`[tool.uv.sources]`) on the import path for REPL sessions.

**Owes:** P22 an API for programs: FastAPI, pydantic, `TypedDict`, first `async def`, the games post scores; "a high-score table… hold that thought until Project 22". P26 the third front end for this same engine (Textual).

## Project 22 · High Score Server

**Taught:** APIs (servers for programs); **type hints at run time**: hints are kept on functions (`get_type_hints`), **`Annotated`** as "a type with a note attached"; **pydantic** `BaseModel` ("a dataclass that checks"), conversion and refusal, `ValidationError`, `Field(ge=, le=)`, `StringConstraints` (pattern, strip, lengths), model inheritance, `model_dump`, `datetime` fields, "use pydantic at the edges"; SQL indexes, a ranking subquery, `COALESCE`, `GROUP BY`; **FastAPI**: `APIRouter` and a factory, **body/path/query parameters decided by the signature**, `Query(ge=, le=)`, response models from the return hint, `status_code=201`, **422** and the shape of its error list, **`Depends`** and **dependencies with `yield`** (= fixture = `@contextmanager`), `contextlib.closing`, `Request` and `app.state`, `check_same_thread=False` and why (sync views run in a thread pool), `StaticFiles`, `FileResponse`, `include_in_schema`, **`/docs` and OpenAPI** ("the documentation can't be out of date"), `fastapi dev main.py`, `curl -X POST … -d`; **`TestClient`**, testing the refusals, pinning the list of paths; **`TypedDict`** (deferred from P17) and the **table of three record kinds** (dataclass / TypedDict / BaseModel: when each is checked); **a first `async def`**: `await`, the event loop, `asyncio.run`, `asyncio.gather`, `asyncio.sleep` vs `time.sleep`, **async generators**, `anext`, `aclose`, **def vs async def in FastAPI** and never blocking the loop (`asyncio.to_thread` named), **server-sent events** with `StreamingResponse`; JavaScript's `<canvas>`, `fetch`, `EventSource`, read as a Python programmer; a game as a **client**: time-outs, never breaking when the server's away, **edge detection**, `types.SimpleNamespace` as a stand-in, `getpass.getuser`; strict mode is comfortable again with typed libraries; **httpx2** named as httpx's successor. Tools: **API tests**; **semantic versioning**, annotated **tags**, `git push origin v1.0.0`, **`gh release create --generate-notes`**, tag commands. Bug hunt: **routes are tried in order** (`/scores/recent` swallowed by `/scores/{game}`); "is my code being run at all?".

**Hands on (the `high_scores` package):** `create_app(database=Path("scores.sqlite3"))`; API v1.0.0: `POST /scores` `{game, player, score}` → 201 `Score` `{game, player, score, id, when, rank}`; `GET /scores/{game}?limit=` → `list[Score]`; `GET /games` → `[{game, plays, best}]`; `GET /scores/{game}/events` (SSE); `GET /` the canvas board; `high_scores.models`: `GameName`, `PlayerName`, `NewScore`, `Score`, `GameSummary`; `high_scores.store`: `connect`, `init`, `add`, `top`, `games`, `newest`, `RANKED`; `high_scores.app`: `router`, `get_db`, `Db`, `Limit`, `changes`; `main.py` for the `fastapi` command. **`snake-online/`** (a nested project, as P11's was): `snake_online.scores` (`post_score(game, score, client=None) -> ScoreRow | None`, `ScoreRow`, `ordinal`, env `SCORES_SERVER`, `PLAYER`) and `snake_online.app.Reporter`. `solutions/signing.py`: HMAC-signed scores.

**Repo notes:** FastAPI is in the root dev group for the Predicts. The live board and `/docs` were checked, and photographed, in headless Chrome.

**Owes:** the PyScript/pygbag bonus next. P23 `datetime`, `subprocess`, Rich. P25 asyncio properly: tasks, `TaskGroup`, exception groups, threads vs processes vs async (and the threaded score post from Extend 3). P27 publishing a package, changelogs, releases in depth.

## Bonus · Share it

**Taught (optional; nothing later may depend on it):** WebAssembly and Pyodide in a paragraph; **PyScript 2026.7.3**: `<script type="py" src= config=>`, `pyscript.toml` `[files]`, `from pyscript import document, when`, calling the browser's canvas from Python, `@when("click", …)` as another registering decorator, **top-level `await`**, why a tab's single thread forces the loop to be `async` (P22's lesson made visible), what Pyodide can't do; **pygbag 0.9.3**: `async def main()` + `await asyncio.sleep(0)` + `asyncio.run`, `uvx pygbag folder` and `--build`, `main.py` in its own folder, click-to-start, Python 3.12/pygame-ce 2.5.7 in the browser, porting caveats. P6's `life` package runs unchanged (third front end; third payoff of pure rules after P13 and P21). Flagged as the least settled technology in the tutorial. No Predict, bug hunt or solutions, by design.

**Hands on:** nothing. `projects/22x-share-it/` is a flat project: `life-in-a-tab/` (page, `pyscript.toml`, `main.py`), `make_site.py` (assembles `site/` from the page and P6's package), `cube-in-a-tab/main.py` (P14's type-in cube, made async).

**Repo notes:** both pages were run in headless Chrome via `scripts/browser.py`, and both pictures are real screenshots. pygbag's loader can hang the DevTools connection for a while, so that check needs hard time-outs. `build/` is git-ignored.

**Owes:** nothing. Part 5 next: P23 Rich Dashboard.

## Project 23 · Rich Dashboard

**Taught:** **`subprocess.run`**: a list not a string, `capture_output`, `text`/`encoding`, **`check=True`** and `CalledProcessError`, `timeout` and `TimeoutExpired`, exit statuses, stdout vs stderr, `shutil.which`, **why never `shell=True`** (the third appearance of "data pasted into code": HTML P18, SQL P20, shell here), looking for a program's machine-readable output (`git log --format=%aI`, `status --porcelain`, `-C`, `-- .`); **`datetime`**: `date`/`time`/`datetime`/`timedelta` and their arithmetic, **naive vs aware**, `datetime.now(UTC)`, work in UTC and convert at the edges with `astimezone()`, **ISO 8601** with `fromisoformat`/`isoformat`, `.date()`, **`total_seconds()` vs the misleading `.seconds`** (the bug hunt), floor-dividing timedeltas, `strftime` codes inside f-strings, `zoneinfo` named, passing `now` in as a parameter; `rglob` with a set intersection to skip `.venv`, `re.MULTILINE`, set comprehensions, `Counter.most_common`, **`defaultdict`**; **sorting**: `key=`, **`attrgetter`/`itemgetter`** (several names give a tuple), negative keys vs `reverse=True`, keys for missing values, **stable sorts** and two-pass multi-key sorting, a **dictionary of key functions** feeding `argparse` `choices`; `f"{n:,}"`; **Rich**: renderables that nest, `Table` (columns, justify, `expand`, `add_section`), `Text` and `Text.assemble` (and why names go in as `Text`, not markup), `Panel`, `Group`, `track(transient=True)`, **`Live`** as a context manager, `Console(record=True)` with `export_text` for tests and **`export_svg`** for the chapter's picture; **factory fixtures** (a fixture that returns a function), repeatable Git repos via `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE`, `monkeypatch` of `shutil.which`. Tools: `rich.inspect`, `rich.traceback.install(show_locals=True)`, `RichHandler`, `python -m rich`. Bug-hunt lesson: test boundaries, and values made of more than one part.

**Hands on (the `dashboard` package, command `dashboard [root] [--sort …] [--weeks N] [--watch SECONDS]`):** `dashboard.git`: `run_git`, `GitError`, `is_tracked`, `commit_times`, `branch`, `changed_files`; `dashboard.when`: `ago(then, now)`, `weekly(times, now, weeks)`, `sparkline(counts)`; `dashboard.stats`: `Project`, `python_files`, `measure`, `scan`, `find_projects`, `busiest_day`; `dashboard.view`: `SORTS`, `table`, `dashboard`. It works whether each project is its own repository or they all share one (this tutorial's repo: 26 projects, 17,712 lines, 655 tests at the time of writing). `solutions/streak.py`: `longest_streak`, `current_streak`.

**Repo note:** the chapter's picture is written by Rich itself (`scripts/screenshots_p23.py` → `export_svg`), from made-up but plausible histories.

**Owes:** P24 Textual ("to the terminal what a page is to a browser's window"), reusing `pyfax.page`; descriptors via reactive attributes; snapshot tests. P25 threads and `ThreadPoolExecutor` explained (Extend 3 uses it blind), asyncio properly.
